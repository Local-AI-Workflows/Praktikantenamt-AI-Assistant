# Email Workflow Validator - Secrets Configuration
# Copy this file to .env and fill in your credentials
# The .env file is gitignored and will NOT be committed

# Gmail App Password (used for both SMTP sending and IMAP receiving)
# Generate at: https://myaccount.google.com/apppasswords
# Steps:
#   1. Enable 2-Step Verification
#   2. Go to App Passwords link above
#   3. Select "Mail" and your device
#   4. Copy the 16-character password
#   5. Paste below (remove spaces)
GMAIL_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx

# Note: Currently using same Gmail for both sending and receiving
# Later when you have separate test inbox:
# Uncomment and set: IMAP_PASSWORD=your-test-inbox-password
