{
  "mcpServers": {
    "company-lookup": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-remote",
        "http://localhost:8081/sse",
        "--transport",
        "sse-only"
      ]
    }
  }
}
